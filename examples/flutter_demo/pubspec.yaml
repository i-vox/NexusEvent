name: nexusevent_flutter_demo
description: NexusEvent Flutter SDK示例应用

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # 本地开发时使用path依赖
  nexusevent_flutter:
    path: ../../packages/nexusevent_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true